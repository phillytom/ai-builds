# Copy to .env.local and fill in. Never commit real values.
# Add new variable names here (names only) as sessions need them.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Neon Postgres connection string (pgvector enabled)
DATABASE_URL=
